[defaults]

inventory = ./inventory/aws_ec2.yml

host_key_checking = False

remote_user = ec2-user

private_key_file = ~/.ssh/ecommerce-project-key.pem

roles_path = ./roles

retry_files_enabled = False

stdout_callback = yaml

interpreter_python = auto_silent

timeout = 30
